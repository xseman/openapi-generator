name: {{pubName}}
version: {{pubVersion}}
description: {{pubDescription}}
{{#pubHomepage}}homepage: {{pubHomepage}}
{{/pubHomepage}}{{#pubRepository}}repository: {{pubRepository}}
{{/pubRepository}}
environment:
  sdk: '{{{sdkConstraint}}}'

# This package intentionally has no runtime dependencies.
# HTTP transport is dispatched through a pluggable HttpSender; the
# default implementation uses dart:io and works on the Dart VM and
# Flutter mobile/desktop targets. Browser users should supply their
# own HttpSender via Configuration.httpSender.

dev_dependencies:
  lints: ^3.0.0
  test: ^1.24.0
